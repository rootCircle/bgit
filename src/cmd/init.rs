use crate::config::{global::BGitGlobalConfig, local::BGitConfig};

pub(crate) fn init(_bgit_config: &BGitConfig, _global_config: &BGitGlobalConfig) {
    unimplemented!("So, sorry! But `bgit init` hasn't been implemented yet!");
}
