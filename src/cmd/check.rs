use crate::config::{global::BGitGlobalConfig, local::BGitConfig};

pub(crate) fn check(_bgit_config: &BGitConfig, _global_config: &BGitGlobalConfig) {
    unimplemented!("So, sorry! But `bgit check` hasn't been implemented yet!");
}
